import classNames from "classnames";

import './index.scss';

const HorizontalLine = () => {
    return(
        <div className={classNames('horizontal-line')}></div>
    )
}

export default HorizontalLine;

